# D16 — No-class-weights ball retrain on the i7 frame (paired twin) — LANDED

**Date:** 2026-07-31 · **Claim:** `ba9cd3f` · **Plan:** `6b58ca7` ·
**Implement:** `ea4acdb` · **Executor result:** `0c569cf` ·
**Handoff:** `research/handoff/D16/` (plan, result.md, full raw evidence)

## Hypothesis

D6's structural question, untested since its CRASH: `balanced` class weights
corrupt the sim's conditional outcome distributions (E5: P(wicket) ~2× actual
teacher-forced, runs/ball +0.38), and every fix to date is post-hoc marginal
patching (E5 vector → A8 → A14/A15 → A16 → B7 → B8, chain closed). Training
with uniform sample weights lets the booster estimate P(outcome|state)
directly — balanced weights only help ranking, which a generative sim doesn't
need. D6 crashed because the legacy `data/xgb_data_v3` frame fail-closes under
the I7 venue-alias contract; the i7 frame (`data/xgb_data_i7`) is trainable, so
D16 ran the test there as a paired twin with zero frame confound: control
(weights ON) + fresh val-fit vector calibrator vs no-weights RAW — the
deployed-stack design transplanted to i7, with the calibrator asymmetry as the
point.

## What was done

- Recovered the D6 trainer flags verbatim (`git checkout 5fb16bb --
  scripts/xgboost_v2.py`; delta verified identical to the 5fb16bb patch).
  New `scripts/auto/d16_{marginal_audit,fit_vector_calibrator,gate_analysis}.py`
  (copy-adapted; `fit_i5_ball_calibrator.py` could not be reused — it
  hard-refuses non-I5 training contracts). Gate script committed before any
  result (`ea4acdb`).
- Twin trainings on `data/xgb_data_i7` (config JSON extracted programmatically
  from `experiments/configs/xgb_i7_venue_identity.yaml` via
  `build_training_cmd` — zero hand-transcription; `--model-dir` on both).
  **Hard check PASS**: all six sidecars byte-identical across arms.
  **Determinism control**: the control booster and all four encoders
  byte-match the archived `models/xgb_i7` (md5 `32b81888…`) — the only diff is
  a pure schema addition in `outcome_dist_config_i7.json`. Early stopping cut
  the no-weights arm at best_iteration **24** (125 trees, 38 MB) vs control
  **443** (444 trees, 134 MB) — anticipated as part of the structural change.
- Fresh `VectorScalingCalibrator` fit on the control arm's venue-ON val
  predictions (124,292 balls; raw LL 1.6376 → 1.5115; max residual 1.4e-10).
- Teacher-forced marginal audits frozen BEFORE the sim evals; twin sim evals
  at seed 46, n=261×100, `--stats-version i7`, banners verified on both arms
  (i7 sqlite cache, 373-venue encoder, B10 selector), 0 skips
  (2251.8 s control+vec / 1297.1 s noweights-raw — the 1.7× speedup is the
  smaller booster).

## Numbers (verbatim from `research/handoff/D16/raw/`)

| Gate | Statistic | Result |
|---|---|---|
| 1(a) | no-weights marginal audit (served venue-ON, 186,667 test balls; frozen pre-eval) | **PASS** — ΔP(wkt) **+0.00319** (tol ±0.005), Δruns/ball **+0.01093** (tol ±0.05), ball LL **1.4253** |
| 1(b) | pooled tail dBrier {pp_total 45/50/55, bowler_wkts_1plus}, 4673 rows | **−0.0116 [−0.0159, −0.0073]** CI-clean better |
| 1(c) | batter_runs_mae (no-regress bar) | **−0.5449 [−0.6714, −0.4142]** — IMPROVES CI-clean (14.4354 → 13.8905) |
| 2 | top_bowler dBrier | −0.0004 [−0.0014, +0.0004] ~noise — held |
| 2 | team_first_over_mae dMAE | −0.0898 [−0.2008, +0.0173] ~noise — held |

Marginal-audit context: control raw **FAILS** at +0.07724 ΔP(wkt) /
+0.4394 Δruns-per-ball (the E5 balanced-weights signature, reproduced *larger*
on i7 than the legacy-frame numbers +0.0647/+0.3829); control + d16 vector
passes (−0.00095/+0.0161, LL 1.5072). Ball-task LL ladder: control raw 1.6316 →
control+vector 1.5072 → **no-weights raw 1.4253** (structural arm beats the
calibrated control by 0.0819 LL).

Context scan: **11 CI-clean movers, all favoring the no-weights arm; zero
CI-clean regressions across 33 families.** Largest: batter_runs_mae −0.5449,
team_total_sixes_mae −0.2579, batter_fours_mae −0.0415, bowler_wkts_1plus
−0.0138, bowler_wkts_2plus −0.0076, batter_6plus_six −0.0066.

## Verdict

**LANDED** (pre-committed mapping: GATE 1 MET + GATE 2 MET). Commits
`ea4acdb` + `0c569cf` kept. **Nothing ships**: artifacts stay in
`models/auto/d16/`, the default (legacy v3) sim path is untouched,
`models/auto/b12/detail_b10_s44_n261.json` remains the canonical baseline for
legacy-path ideas, and i7 serving is gated on the human I17 promotion bundle.

The structural claim is confirmed at unusual strength: no-weights RAW — with
no calibrator at all — dominates the deployed-stack design (balanced weights +
val-fit vector), improving the tail-overshoot pool AND batter_runs_mae
(~3.4× the B6 venue-encoder gain on that family) simultaneously. The entire
E5→B8 calibration chain was compensating for a training-time distortion that
simply not applying `balanced` weights removes outright.

## What I'd try next

Two follow-ups appended as PENDING: **D17** — fit a vector calibrator on the
no-weights arm's own val predictions and test no-weights+vector vs no-weights
raw at the same seed (the residuals +0.00319 P(wkt) / +0.00276 six are the
last cheap marginal lever; a null closes the calibration chain for the
structural arm too). **D18** — no-weights-adapted hyperparameters: lr 0.2404 /
444 trees were tuned under balanced weights, and early stopping's cut to
best_iteration 24 says that config is over-aggressive for the uniform-weight
loss surface; a val-LL-selected gentler lr could extend the gain. Beyond the
loop: this result belongs in the human I17 promotion bundle — the i7 ball
model of record should plausibly be a no-weights retrain, not the archived
balanced-weights `models/xgb_i7`.
