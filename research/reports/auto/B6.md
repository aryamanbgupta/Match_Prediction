# B6 — Venue-encoder fix re-gated on batter-level continuous primary at a fresh seed (B1 follow-up)

**Verdict: LANDED** (2026-07-12). Both pre-committed gate conditions met at
fresh seed 43; encoder sidecar shipped into `models/xgb_v3/` — the default
sim path is now venue-ON. **All future sim comparisons must re-baseline.**

## Hypothesis

B1 fixed the sim's venue blindness (`venue_encoded=0` on every sim ball →
real training codes via a training-time venue encoder) but TABLED: its
pre-committed venue-binary pooled primary straddled 0, while the CI-clean
effect concentrated in **batter-level** families — above all
`batter_runs_mae` −0.175 [−0.298, −0.048], E2's single validated sim skill —
with zero families significantly worse anywhere. Because that batter-level
primary was identified *post hoc* on B1's seed-42 run, it had to be confirmed
on fresh Monte Carlo draws where the selection cannot leak. B6 = same
implementation, new pre-committed primary, fresh seed.

## What I did

1. Re-applied B1's implementation verbatim (`11a19ea`, revert of revert
   `c37f1b0`, byte-identical to `112c59e`): `XGBoostModelV2` gains a
   `venue_encoder_path` param + auto-detects a `venue_encoder_v3.pkl` sidecar
   next to the model file. Encoder artifact `models/auto/b1/venue_encoder_v3.pkl`
   (467 venues, rebuild-proven training-exact in B1).
2. Pre-committed the gate (`422c266`, `scripts/auto/b6_gate_analysis.py`)
   **before any seed-43 result existed**: PRIMARY = `batter_runs_mae` paired
   dMAE < 0 with 95% CI excluding 0; guards = `top_bowler`,
   `bowler_wkts_{1,2,3}plus`, `team_first_over_mae`,
   `team_total_{fours,sixes}_mae` (no CI-excludes-0 increase). One pre-named
   primary, no scan — the A16 lesson.
3. TWO fresh recipe-B runs at **seed 43** (a prior iteration's runs were
   wall-clock-killed at startup; this iteration re-ran both):
   - venue-blind: default `models/xgb_v3/` model (startup log: "venue encoder
     absent (venue_encoded=0)"), 2226.3 s → `models/auto/b6/detail_blind_s43_n261.json`
   - venue-on: `--model-path models/auto/b1/xgboost_model_v3.pkl` (startup
     log: "venue encoder ACTIVE (467 venues)"), 2215.9 s →
     `models/auto/b6/detail_venue_s43_n261.json`
   Both n=261×100, `--ball-calibrator vector`, identical except the encoder.
   Neither side reuses any seed-42 draw, so B1's post-hoc selection cannot
   leak into this comparison.
4. Paired Brier/MAE (venue − blind), cluster-bootstrapped by match
   (`scripts/auto/b6_gate_analysis.py`, reusing the a8 tooling; output saved
   verbatim at `models/auto/b6/gate_analysis_output.txt`).

## Results (n=261×100, seed 43, paired venue − blind)

### GATE 1 — primary (decisive)

| family | n obs | MAE blind | MAE venue | dMAE | 95% CI | verdict |
|---|---:|---:|---:|---:|---|---|
| **batter_runs_mae** | 3176 | 15.006 | 14.844 | **−0.162** | **[−0.287, −0.036]** | **IMPROVED (CI < 0)** |

B1's seed-42 estimate was −0.175 [−0.298, −0.048]; seed 43 reproduces it
almost exactly → the batter-level gain was real, not selection noise. **MET.**

### GATE 2 — guards (must not regress)

| guard | dBrier / dMAE | 95% CI | flag |
|---|---:|---|---|
| top_bowler | +0.0004 | [−0.0002, +0.0011] | noise — held |
| bowler_wkts_1plus | −0.0037 | [−0.0064, −0.0008] | IMPROVED (CI-clean) |
| bowler_wkts_2plus | −0.0011 | [−0.0032, +0.0010] | noise — held |
| bowler_wkts_3plus | −0.0001 | [−0.0009, +0.0006] | noise — held |
| team_first_over_mae | +0.012 | [−0.031, +0.055] | noise — held |
| team_total_fours_mae | −0.018 | [−0.079, +0.044] | noise — held |
| team_total_sixes_mae | −0.001 | [−0.062, +0.059] | noise — held |

No guard regresses with CI excluding 0. **HELD.**

Both conditions met → **LANDED** per the pre-committed mapping.

### Context scan (cannot flip the verdict; reproduces B1's one-sided pattern)

CI-clean better at seed 43: `batter_50plus` −0.0020 [−0.0031, −0.0009],
`batter_fours_3plus` −0.0030 [−0.0051, −0.0009], `batter_fours_mae` −0.016
[−0.030, −0.002], `team_highest_individual_ou_29_5` −0.0026 [−0.0058, −0.0001]
and `_34_5` −0.0053 [−0.0095, −0.0015], `highest_over_runs_ou_18_5` −0.0150
[−0.0247, −0.0058], `pp_total_ou_50_5` −0.0081 [−0.0150, −0.0014].
Significantly worse: **none** (0 families anywhere in the scan). The effect
again concentrates at the batter level — venue codes sharpen per-batter
forecasts; multi-over team aggregates wash out the per-ball correction
(A8/A12/A16 pattern). B1's seed-42 co-movers largely reproduce
(batter_fours_1plus −0.0033 just misses at [−0.0066, +0.0001], within
expectation for a borderline family across seeds).

## Ship (pre-committed LANDED action)

- `models/auto/b1/venue_encoder_v3.pkl` → `models/xgb_v3/venue_encoder_v3.pkl`
  (new sidecar file; nothing overwritten; `models/` is gitignored — artifact
  reproducible via `scripts/auto/b1_build_venue_encoder.py`).
- Smoke-verified: default `--model-path models/xgb_v3/xgboost_model_v3.pkl`
  now prints "venue encoder ACTIVE (467 venues)".
- **RE-BASELINE WARNING (B1's note, now in force):** every historical sim
  detail JSON (e.g. `models/auto/a8/detail_vec_n261.json`, A14/A15/A16
  baselines) is venue-BLIND. Any future sim comparison must pair against a
  venue-ON baseline. Canonical fresh baseline:
  `models/auto/b6/detail_venue_s43_n261.json` (seed 43) — its venue-blind
  twin `detail_blind_s43_n261.json` is kept for reference.

## What I'd try next

The stale-calibrator question: `vector_scaling_calibrator_v1.pkl` (E5) and
the A15 over-0 calibrator were both fit on *venue-blind* val predictions; the
model's input distribution just changed under them. Refitting both on
venue-on val balls and re-validating A15's first-over gain against the new
venue-on baseline is the clean next step (appended as B7). Beyond that, the
batter-level sharpening directly feeds B3 (continuous-forecast shrinkage
blend) — the sim's validated MAE edge just grew, so the blend's α should
shift toward the sim; B3 should be run against the venue-on sim only.
