# B12 — B10 selector re-gated on `bowler_wkts_1plus` primary at fresh seed 44

**Verdict: LANDED** (2026-07-31, claim `c35cd8b`, implement `7617ec3`+`a50f905`)

## Hypothesis

B10 (TABLED, seed 43) share-matched the usage-absent branch of
`EmpiricalBowlerSelector` to B9's as-of expected-balls share. Its
pre-committed `top_bowler` primary was flat, but the guard scan surfaced a
CI-clean improvement exactly where D15 left its residual defect —
`bowler_wkts_1plus` −0.0049 [−0.0075, −0.0023] — with zero families CI-clean
worse anywhere. Because that signal was identified post hoc on seed-43
draws, it required confirmation on fresh Monte Carlo draws with a
pre-committed primary (B1→B6 precedent).

## What was done

- Re-applied B10 verbatim: `7617ec3` = revert of `a8c061b` (which had
  reverted B10's implement `ad144ea`); `git diff ad144ea HEAD` over
  `sim_v1_2.py` + the three `b10_*.py` scripts verified EMPTY.
- `b10_unit_check.py` fully passed twice (pre-run and immediately before
  Arm B): d15 30/30, legacy parity float-exact with `_b10 is None` on the
  default payload, exp_balls parity vs `b9_usage_baseline.AsOfUsage` at
  0.0e+00, weight table reproduced, production md5 unchanged at eval time.
- Pre-committed `scripts/auto/b12_gate_analysis.py` (`a50f905`, 12:36) with
  the retooled primary/guards BEFORE any eval output existed (first detail
  written 13:12).
- TWO fresh recipe-B runs at **seed 44** (blind 2150.3 s / b10 2149.6 s,
  261/261 both, sequential): startup banners confirm venue-ON, empirical
  selector, run-out channel, vector calibrator on both arms; B10-ACTIVE
  line on Arm B only. Only delta = the usage-absent selector weighting.

## Numbers (paired b10 − blind, cluster-boot 2000 draws, boot seed 29)

| family | n | blind | b10 | delta | 95% CI | reading |
|---|---|---|---|---|---|---|
| **bowler_wkts_1plus (PRIMARY)** | 3106 | 0.2578 | 0.2532 | **−0.0046** | **[−0.0071, −0.0021]** | CI-clean better → GATE 1 MET |
| top_bowler (guard) | 5831 | 0.0773 | 0.0771 | −0.0002 | [−0.0008, +0.0005] | held |
| bowler_wkts_2plus (guard) | 3106 | 0.2154 | 0.2145 | −0.0009 | [−0.0024, +0.0007] | held |
| batter_runs_mae (guard) | 4253 | 14.3905 | 14.4030 | +0.0125 | [−0.0427, +0.0683] | held |
| team_first_over_mae (guard) | 522 | 3.4103 | 3.4225 | +0.0122 | [−0.0006, +0.0246] | held |

GATE 1 MET + GATE 2 MET (no CI-clean guard regression) → **LANDED** per the
pre-committed mapping.

The seed-43 signal **reproduces on fresh draws**: −0.0049 [−0.0075, −0.0023]
at s43 → −0.0046 [−0.0071, −0.0021] at s44 (cf. B1→B6: −0.175 → −0.162).

Context (verbatim from `research/handoff/B12/raw/gate_output.txt`):
- Full 32-family scan: only two CI-excludes-0 deltas, BOTH improvements —
  the primary and `highest_over_runs_ou_24_5` −0.0014 [−0.0030, −0.0000];
  **zero families CI-clean worse**. `first_wicket_runs_ou_30_5` did not
  reproduce (−0.0022 [−0.0044, +0.0000], boundary/noise).
- G5 coverage: blind 3095/3108 = 0.9958, b10 3105/3108 = 0.9990 (identical
  to seed 43).
- B9 sim−usage top_bowler margin: +0.0029 blind → +0.0027 b10 (s43:
  +0.0028 → +0.0026) — the margin stays essentially open; B10/B12 fixes
  defect (a) (debutants), not defect (b) (never-bowlers) → B13.
- 8 relaxation triggers (same count as seed 43, benign).
- Honest flag: the *positional* cross-check on the primary is ~noise at
  s44 (−0.0020 [−0.0060, +0.0020]) where it was CI-clean at s43
  (−0.0041 [−0.0078, −0.0005]). The pre-committed verdict statistic is the
  identity-keyed pairing, which is the sharper and correct construction
  post-B2/I15; the positional variant mixes attribution across card slots.
  Recorded, does not gate.

## Ship (pre-committed action on LANDED)

The default eval path is pinned by argparse defaults in
`prop_backtest.py`/`run_sim_eval.py` (eval framework, untouchable) to
`models/bowler_phase_usage.json`, so the ship adds the opt-in key to that
payload itself (not on program.md's protected-artifact list; the earlier
"don't touch" rule was executor-scoped pre-landing safety):

- `models/b10_usage_corpus.pkl` = stable byte-copy of the B10 as-of corpus
  (md5 `42b313b138fefdd293c280a9d4be2e2b`; rebuildable via
  `b10_build_usage_sidecar.py` / `b9_usage_baseline.py`).
- `models/bowler_phase_usage.json` += `b10_asof_usage`
  {corpus_path: models/b10_usage_corpus.pkl, k_usage 5.0, min_eligible 5,
  min_share 0.01}; deep-equality of the sidecar minus the key vs production
  asserted before writing. md5 `ea0c73d3ddb48f499b6273f9a397b0e3` →
  `2e650423f0c949631fca1f15dd1c8a56`; pre-ship backup at
  `models/auto/b12/bowler_phase_usage_pre_b12.json`.
- Smoke: `EmpiricalBowlerSelector()` on the DEFAULT path prints
  `B10 usage-aligned bowler selector ACTIVE (k_u=5.0)`, corpus resolves
  (7433 players).

**RE-BASELINE IN FORCE:** the default sim path is now b10-active. Canonical
default-path baseline = `models/auto/b12/detail_b10_s44_n261.json` (seed 44;
the s44 blind twin and the s43 b10/blind pair are kept). All pre-B12
blind-arm details are pre-ship lineage only.

Known post-ship caveats:
1. `b10_unit_check.py`'s md5 assertion now (correctly) fails — it pins the
   pre-ship payload. Any future re-run must compare against the post-ship
   md5 or the backup copy.
2. `scripts/build_bowler_phase_usage.py` regenerates the payload WITHOUT
   the b10 key — a rebuild would silently revert the shipped fix (the only
   tell is the missing ACTIVE banner in run logs). Queued as I20.
3. D5 (bowler eligibility filter) queue status is now a supervisor call per
   B12's gate text: who-bowls share alignment is shipped; what remains of
   D5 is the per-ball-rate tail (B4's non-bowler p_sim≥2% rows), which B13
   targets from the weights side.

## What I'd try next

B13 (never-bowler damping) is the natural sequel and its blind arm should
now be the shipped default (its idea text already anticipates this). The
sim−usage top_bowler margin (+0.0027) remains the open scoreboard: B12
closed the debutant side of who-bowls; the veteran-never-bowler side (B10
made it slightly worse: 0.270%→0.496% share) is untouched and is exactly
where B13's beta-binomial damping should bite. If B13 also lands, re-run
the B9 margin — if it still doesn't close, the residual is per-ball-rate,
not who-bowls, and the lever moves to D6.
