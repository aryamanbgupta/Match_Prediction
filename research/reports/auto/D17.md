# D17 — Val-fit vector calibrator on the no-weights i7 arm

**Verdict: FAILED** (2026-07-31; claim 2026-08-01T01:08:57Z UTC) — and this is
the *useful* kind of FAILED: the pre-registered decision-grade null. The
marginal-calibration chain (E5 → A8 → A14/A15 → A16 → B7 → B8 → **D17**) is
now closed for the structural arm. **No-weights RAW is certified as the final
i7 ball stack for the I17 bundle** — no calibrator belongs on top of it.

## Hypothesis

D16's no-weights retrain passes the teacher-forced marginal audit RAW but
with nonzero residuals (ΔP(wkt) +0.00319, Δruns/ball +0.01093). A
`VectorScalingCalibrator` fit on the no-weights arm's own val predictions is
the one remaining cheap marginal lever; either it buys a further tail win, or
the null certifies RAW and closes the chain.

## What was done

- Fit the calibrator with the existing (unedited) `d16_fit_vector_calibrator.py`
  pointed at `models/auto/d16/noweights` → `models/auto/d17/vector_scaling_calibrator_d17.pkl`.
  Encoder cross-check identical on all 4 encoded columns; fit residual 2.8e-11.
  Val ball LL 1.433437 → 1.433238 (−0.0002 — the first sign there was nothing
  to buy).
- **Pre-run expectation check** (committed before the eval): the fitted vector
  is *nearly the identity*. `VectorScalingCalibrator` normalizes `v` to sum 1
  (`calibration.py:440`), so identity = 1/6 per element and the plan's literal
  `max |v−1|` is degenerate (~0.83 for any vector); the executor recorded both
  readings. On the comparable scale: **max |6v−1| = 0.0545**, driven solely by
  the wicket class (−5.4%); the other five classes sit below the ~0.05 washout
  threshold. For contrast, D16's control arm needed 0.9508 — the D16
  structural result restated in calibrator parameters. Marginal "live test"
  call; eval run anyway.
- ONE recipe-B eval, seed 46, n=261×100, `--stats-version i7`, exactly D16
  Arm-N settings + the d17 calibrator (banners verified: d17 vector path, i7
  SQLite cache, venue encoder ACTIVE 373, B10 selector k_u=5.0). 1309.8 s,
  261/261 matches, 0 skips. Paired cluster-boot (2000 resamples, seed 29) vs
  the existing `models/auto/d16/detail_noweights_raw_s46_n261.json` — same
  seed, same engine, only delta = the calibrator (B7 pairing precedent). Gate
  script `d17_gate_analysis.py` committed at `538ca24` before any eval output.

## Numbers (delta = noweights_vec − noweights_raw; negative = calibrator better)

| stat | n | delta | 95% CI | call |
|---|---|---|---|---|
| GATE 1(i) pooled tail {pp_total 45/50/55, bowler_wkts_1plus} | 4673 | **+0.0011** | [−0.0001, +0.0022] | ~noise, wrong sign → **FAIL** |
| GATE 1(ii) batter_runs_mae (must not regress) | 4254 | **+0.1439** | [+0.0942, +0.1949] | CI-clean WORSE (13.8864 → 14.0303) → **FAIL** |
| GATE 2 guard top_bowler | 5833 | +0.0001 | [−0.0003, +0.0005] | held |
| GATE 2 guard team_first_over_mae | 522 | +0.0166 | [−0.0002, +0.0337] | held |

Verdict per the pre-committed mapping: pooled tail not CI-clean negative →
FAILED regardless of guards.

Context scan (33 families, 10 CI-clean movers) — a **redistribution, not a
gain**. Shaving the wicket class flatters aggregate totals at the cost of
per-player resolution:

- Better (6, all team/innings aggregates): innings_runs 160/170/180
  −0.0089/−0.0094/−0.0078, highest_over_18_5 −0.0067, first_wicket_30_5
  −0.0040, bowler_economy_8_5 −0.0023.
- Worse (4, all per-player): bowler_wkts_1plus +0.0028, bowler_wkts_2plus
  +0.0017, batter_fours_mae +0.0117, batter_runs_mae +0.1439.

The batter_runs_mae regression is the exact trade the E5-era calibrator
historically lost (it breaks the boundary/wicket cancellation), reproduced
here on the structural arm — further confirmation the calibrator mechanism,
not the balanced weights alone, causes that cost.

**Honest methodological note:** the pre-committed row pool is 66.5%
`bowler_wkts_1plus` rows (3107/4673), the one pool family that regresses
CI-clean; equal-weight-per-family pooling would read −0.0010 (opposite sign).
The row-pool weighting was pre-committed and the verdict stat is the
pre-committed one — and GATE 1(ii) fails CI-clean regardless, so no
alternative weighting rescues the idea.

## Integrity

Purely additive iteration: commits `538ca24` (implement, before eval) +
`a25bcd3` (result) touch only `scripts/auto/d17_gate_analysis.py` and
`research/handoff/D17/`. Baseline detail JSON, the D16 no-weights booster,
and protected `models/xgb_i7` / `models/xgb_v3` boosters all md5-verified
byte-identical before/after. Default (legacy v3) sim path untouched;
calibrator loads only via runtime `--ball-calibrator-path`. Nothing reverted,
nothing shipped. Evidence: `research/handoff/D17/` (result.md + raw logs with
`.txt` twins).

## What I'd try next

Nothing in this direction — that's the point of the null. The chain
E5 → A8 → A14/A15 → A16 → B7 → B8 → D17 is closed at both ends: linear
marginal scaling is exhausted on the legacy engine (B8) and now provably
unnecessary on the structural arm (D17). The surviving i7 ball-stack levers
are already queued: **D18** (no-weights-adapted hyperparameters — lr 0.2404
was swept under balanced weights and early stopping cut 444 → 24 trees, so
the config is over-aggressive for the new loss surface) and **A13**
(dispersion/variance calibration — an orthogonal defect class that marginal
scaling never addressed, and the known cause of the B5/B15 band-width
problem). No new queue ideas appended.
