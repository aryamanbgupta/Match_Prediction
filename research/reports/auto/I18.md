# I18 — i7-identity golden frame + swap-i7 golden audit (I17 promotion gate)

**Verdict: LANDED (instrumentation gate; readout is DESCRIPTIVE by
predeclaration).** 2026-07-30 night loop. Claimed `d3e4d64`; implementation
`7bdd052` + `6b9fa11`; handoff `4fd4bb9` + `c4258ae`.

## Purpose

Golden-audit readout for the I17 promotion decision: build the golden
match frame under the I7 identity contract and score the FIXED successor
candidate (`models/auto/i17/swap_seed29`) against its control
(`base_seed29`) on the 124-fixture golden set. Golden is audit-only; the
candidate was pre-specified; nothing here fed selection. The supervisor
authorized a scoped read-only golden exception in the I18 queue entry
(commit `bdb1131`, "Night-executable").

## What was done

1. **Preconditions:** `verify_forward_holdout` PASS (137 selected / 401
   context, overlap 0, fingerprint `82ccde16cf2b…`). Determinism
   spot-check: `base_seed29` vs archived `models/xgb_match_i7`
   test predictions max |Δp| = **0.0**.
2. **Frame:** `materialize_match_features.py --version i7
   --extra-source-dir data/golden/t20s_json` (168 s; train 7,972 / val 528
   / test 798 / **golden_test 227**), then `build_i7_match_frame.py` →
   48-column production contract at `data/auto/i18/frame/`.
3. **Gate A ruling 1 (schema drift):** the strict parity gate vs the frozen
   `data/xgb_match_data_i7` failed on column count (57 vs 54) — but all 53
   shared columns are **bit-identical** on train/val/test; the delta is
   exactly the I15/I16 identity set (`match_id` semantics swapped
   display-string → cricsheet-id, verified `reference.match_id ==
   candidate.display_match_id` on 100% of rows, plus
   `display_match_id`/`match_identity_version`/`elo_update_version`), and
   `venue_identity.json` is identical. Ruled a PASS of a relaxed parity
   contract (encoded in `scripts/auto/i18_frame_parity.py`), **but the
   sanctioned copy of `golden_test.parquet` into `data/xgb_match_data_i7/`
   was permanently withheld**: the frozen siblings predate I15 and a
   mixed-key directory is the exact silent-join hazard I15 exists to
   prevent. The coherent current-contract frame (all four splits) lives at
   `data/auto/i18/frame/` (gitignored, regenerable via the two commands
   above + the parity harness). Re-keying the frame of record is a human
   decision → I19.
4. **Gate A ruling 2 (mixed-key odds):** `betting_odds_golden.json` is
   mixed-key after the 2026-07-30 extension — the 55 original rows carry
   legacy display ids with no `cricsheet_id` (preserved verbatim by
   design), while the 69 extension rows already use cricsheet ids.
   `patch_envelope_cricsheet_ids.py` therefore fails closed (69
   unmatched). Fix was additive: `scripts/auto/i18_stamp_envelope.py`
   verifies the 69 pre-stamped rows and stamps the 55 legacy rows via the
   shared tool's own `raw_display_id`, then requires the 124 ids to be
   unique and exactly equal, as a set, to the 124
   `data/golden/polymarket_test/` stems. Result: **124/124**. The shared
   tool was not modified; do NOT "normalize" the odds file (the original
   55 rows are preserved verbatim by policy) — consumers must handle mixed
   keys; the helper is the reference implementation.
5. **Scoring:** `predict_golden.py` per arm on the 227-row golden split
   (3 unseen venues fell back: Gaborone Oval 2, Korogi Sports Park, Sano
   International 2 — all low-liquidity), blend w=0.0 (**124 blended / 0
   passthrough both arms**), reslice ×3 with
   `--cluster-source-dir data/golden/t20s_json` (I3
   `tournament_time_block_v1`).

## Readout (descriptive; blocks 11/9/5; one no-result fixture excluded)

| arm | slice | bets | LL | market LL | Flat ROI | ROI block CI (n blocks) | win rate |
|---|---|---:|---:|---:|---:|---|---:|
| swap-i7 | all | 123 | **0.5988** | 0.5513 | −1.14% | [−45.24, +23.29] (11) | 48.0% |
| swap-i7 | ≥$50k | 74 | **0.6538** | 0.6573 | +15.70% | [−57.35, +32.24] (9) | 55.4% |
| swap-i7 | ≥$100k | 65 | **0.6767** | 0.6843 | +21.23% | [−100.00, +32.55] (5) | 58.5% |
| base-i7 | all | 123 | 0.6056 | 0.5513 | −17.68% | [−57.14, +1.60] (11) | 34.1% |
| base-i7 | ≥$50k | 74 | 0.6636 | 0.6573 | +1.23% | [−54.20, +11.18] (9) | 45.9% |
| base-i7 | ≥$100k | 65 | 0.6859 | 0.6843 | +4.36% | [−100.00, +10.30] (5) | 47.7% |

Cross-check: market LL (0.5513 / 0.6573 / 0.6843) reproduces
`reports/golden_extension_eval_20260730.md` to 4 dp — independent
confirmation of the join and slicing.

## Reading (for the human promotion decision — descriptive only)

1. **Swap beats base on every slice, both metrics** — LL Δ −0.0068 /
   −0.0098 / −0.0092 and ROI +16.5pp / +14.5pp / +16.9pp. Single seed;
   the sharp-slice LL deltas sit at the ~0.007 seed-noise floor, so treat
   as directionally consistent with I17's 5/5-seed iteration result, not
   as independent proof.
2. **Swap-i7 beats the slice-matched market LL on both sharp slices**
   (0.6538 < 0.6573; 0.6767 < 0.6843); base beats it on neither. Notably,
   the legacy production swap model **trailed** the market on these same
   124 fixtures (≥$50k 0.6685, ≥$100k 0.6938) — the i7-identity candidate
   is ~0.015–0.017 LL better than the legacy line here. Caveat: the two
   lines score from different state contracts (legacy = frozen
   forward-state sidecar; i7 = fresh i7 materialization through the golden
   window), so the cross-line comparison is indicative, not paired.
3. **Both arms trail the all-slice market badly** (0.5988 vs 0.5513) — the
   extension window is WC-heavy and market-sharpest, reproducing the
   legacy extension finding.
4. **No betting edge:** every ROI block CI straddles zero and the sharp
   slices are descriptive (<10 blocks). The +15.70%/+21.23% swap point
   estimates are noted, not relied on.
5. Promotion remains gated on the fresh-state serving cutover plan
   (human). This readout satisfies the golden-audit leg of
   `docs/I17_I7_SWAP_SUCCESSOR.md`'s promotion path.

## Operational notes

- The executor's first materialization died because it was backgrounded
  and reaped at turn end (the D2 pattern at subagent level); the
  foreground re-run took 168 s. Match-frame materialization is minutes,
  not the 30–60 min budgeted — future plans can treat it as cheap.
- Both Gate A blockers were fail-closed stops with clean diagnostics,
  resolved by orchestrator rulings without weakening any integrity
  property. Raw logs: `research/handoff/I18/raw/` (17 files);
  full executor readout: `research/handoff/I18/result.md`.

## What I'd try next

I19 (appended): re-key the successor line's training frame to the current
identity contract in an isolated `_v2` dir and prove the I17 arms
reproduce at max |Δp| = 0 — removes the mixed-contract hazard that forced
this run to withhold its golden split, and pre-clears the frame decision
inside the human promotion bundle (fresh-state cutover plan, frame choice,
then the `predict_fixture.py` switch).
