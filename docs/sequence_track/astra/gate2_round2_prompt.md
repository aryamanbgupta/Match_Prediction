You are Codex Astra, round 2 of the stage 2 RESULTS GATE (read-only). In round 1 you returned `VERDICT: NO SIGN-OFF` with six MUST-FIX and two SHOULD. All eight are now actioned. This is a verification round: confirm or reject each closure, name any regression, and decide whether the report and the acceptance record may be committed.

Read `docs/sequence_track/stage2_acceptance.md` first — its **D11 "Gate 2 round 1" section and the "Gate 2 round 1 fixes" table** record every finding and its closure verbatim, and a new **D12** registers a seed extension launched while these fixes were being made. Then `research/reports/embeddings/SEQ_STAGE2_REPORT.md` (987 lines, re-rendered), `scripts/sequence_track/render_stage2_report.py`, `scripts/sequence_track/stage2_stats.py`, the new `scripts/sequence_track/pin_stage2_analysis.py`, `docs/sequence_track/stage2_analysis_pin.json`, and the test files `scripts/tests/test_render_stage2_report.py`, `test_stage2_stats.py`, `test_pin_stage2_analysis.py`. Do not open `data/golden/`, `data/forward_holdout/` or `models/embeddings/seq_stage2/cohort/`.

## The closures

1. **Dependency certification.** Controls are matched through the recorded `dependency_set_arm`/`dependency_set_k`, never their own arm/k. Coverage-complete requires per masked arm: a passing certificate at **both** training seeds (from the checkpoint directory, not the perturbation seed field), the recorded `checkpoint_md5` **recomputed from `model.pt` on disk and matching**, the registered settings (2000 targets, seed 29, validation), its own S(i), and the registered matched control present and fired; globally both controls. Seven blocking statuses force the arm's family to `NOT_EVALUABLE`, and blocking now propagates to § 7 mechanism and § 11 falsification as well as results and gates, with the report stating that where it differs from the statistics JSON's eligibility the report's block is operative. Certificate loading is recursive. **Your exact attack now fails**: four seed-7 certificates and no controls gives coverage incomplete with all four arms blocked.
2. **Cohort-unlock conditions** render in full, all nine including (0) unresolved historical consumption, plus "five seeds alone cannot unlock this cohort"; a new required § 9 disposition `unresolved_historical_consumption` brings the required list to 21.
3. **The two arithmetic errors and the chase-gate summary** in the acceptance record are corrected (k=0 +0.002470, k=6 +0.001486; chase additionally fails numerically for `full`, `same_entity_k0`, `same_entity_k6`, and with Holm for `aligned_hist_rf` and `same_entity_k12`).
4. **Unsupported conclusions** removed: all three named sentences gone, null wording corrected to "no detected benefit" / "was not established at this resolution". The config's `known_limitations[1]` was **not edited** (training provenance is immutable); the renderer substitutes corrected prose and **discloses the swap with the original wording and the reason**. Judge whether that disclosure mechanism is acceptable or whether it hides a divergence between registration and report.
5. **Coverage** is checked inside § 9 only, against complete normalised entries including consequence clauses; four mutation tests added including your exact truncation attack; a new test ties named cells to their named source fields.
6. **The analysis freeze** is `pin_stage2_analysis.py` writing `eval_out/seq_stage2/analysis_pin.json`, snapshotted to the tracked `docs/sequence_track/stage2_analysis_pin.json`. It records analysis-code hashes, the **exact statistics, ksweep and renderer invocations with every path argument**, evidence hashes, the k selection and the family map; `--verify` rebuilds from the recorded invocation. It does not touch the training config.
7. **Slice identity** now persists a `mask_sha256` and compares digests; the existing statistics JSON predates it, so this report's `innings_2`/`chase` disclosure falls back to counts **and says so**.
8. **Heading and disposition** derive from verified coverage so "pending" and "complete" cannot coexist; matched controls are shown per configuration, with the disclosed gap that `same_entity_k0` and `same_entity_unr` have no separately registered control because D7.4 registers only two.

**A defect your MUST-FIX 6 predicted, found in the orchestrator's own command.** The report had been rendered with `--dependency-dir …/dependency/recert`, which holds the eight trained certificates but **no controls** — they live in the parent. Under the permissive code that produced a report claiming complete coverage; under the fix it correctly blocks. The report of record is now rendered from the evidence root `models/embeddings/seq_stage2/dependency`, which is what the pin records. **Confirm that is the right invocation**, and say whether the smoke certificates being in scope (they read `NOT_AUTHENTICATED_CLOSED_TREE` and never count toward trained coverage) is handled correctly.

**Current coverage**: 14 certificates in scope (8 trained recerts, 4 smoke, 2 controls); all 8 trained pass at exactly 0 with md5s authenticated against disk; both controls matched through their dependency set and fired at 0.7517 and 0.9217. Coverage complete, nothing blocked.

**No statistic changed**: a number-by-number diff removes zero numbers and adds exactly three (`−0.00003`, `−0.00060`, `+0.00044`), re-reads of the existing `residual_t1 − residual_mlp` row in the new null-wording sentence. Suites: 178 passed on the three analysis files; 1754 passed, 56 deselected on the full artifact-free suite.

## Also in scope: the seed extension (D12)

While these fixes were made, both machines were put back to work on the extension you named. Eight whole families — `mlp`, `full`, `fixed_decay`, `fox`, `aligned_hist`, `xlstm`, `residual_mlp`, `residual_t1` — at seeds 29, 42 and 101, registered in a **separate** `experiments/configs/seq_stage2_ext_v1.yaml` so the night-1 config, its provenance block and its COMPLETE markers stay immutable (the driver refuses to widen a registered seed list). Seeds 29 and 42 on the laptop, 101 on the mini, so every contrast stays within-machine at each seed. A third config, `seq_stage2_5seed_v1.yaml`, registers all five seeds for those eight configurations and seven families, for **consolidation and analysis only**, because `--consolidate` rewrites each summary from the config's registered seeds and would otherwise drop a night's seeds from the summary. The ownership and k families are deliberately **not** extended, so no five-seed ownership or window claim will exist. All three configs pin OK. **Verified early: the training signature is identical across every landed seed of all sixteen configurations, zero mismatches, including where seed 29 joins seeds 7 and 13 across two machines and two registrations.**

Questions on this: is the three-config arrangement sound, or does it create a registration ambiguity? Is the by-seed split still valid with three seeds across two machines (29 and 42 laptop, 101 mini)? Does anything about the extension retroactively affect the two-seed report now being committed?

## Output

Confirm or reject each of the eight closures with file:line. Then: (a) is the evidence-root invocation right; (b) is the `known_limitations[1]` substitution-with-disclosure acceptable; (c) is the D12 arrangement sound; (d) may the report and acceptance record be committed as the stage's two-seed screen result, with the five-seed reading to follow as a separate addendum. List any remaining MUST-FIX. End with exactly one line: "VERDICT: SIGN-OFF" or "VERDICT: AGREE WITH CHANGES" or "VERDICT: NO SIGN-OFF".
