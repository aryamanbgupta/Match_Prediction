You are Codex Astra. Two jobs, read-only. The stage 2 orchestrator of the sequence/embeddings track has been told by the user to finish the stage autonomously tonight without asking the user for decisions, and to ask you instead wherever the contract says "after the user's go". So: (1) write the content of two acceptance tables, and (2) rule on one irreversible action.

Read first: docs/sequence_track/stage2_acceptance.md IN FULL (it is the contract: the "Arm register" table, D2.1, D2.6, D5, D6, D7, the amended D8 including 8.9, and the empty D9/D10/D11 sections), docs/SEQUENCE_TRACK_PLAN.md § "Statistical rules for the whole track" and § "Stage 2", docs/sequence_track/stage2_handoff_opus.md § 4 and § 5 steps 6-7, experiments/configs/seq_stage2_v1.yaml (its `statistics` and `cohort` blocks), scripts/registered_experiment.py (`match_ids`, `row_log_loss`, `match_cluster_ci`, `seed_mean_match_cluster_ci`), scripts/sim_eval/eval_statistics.py (`load_competition_clusters`, `BOOTSTRAP_CONTRACT_VERSION`, `MAX_EVENT_GAP_DAYS`, `count_unique_clusters`), scripts/sequence_track/holm_stage1.py, and research/reports/embeddings/SEQ_STAGE1_REPORT.md (for the house style of a stage report, especially its § 9 deviations/asymmetries/limitations and its § 10 plain-language section).

## Situation

Sixteen registered configurations, two seeds each (7 and 13), trained teacher-forced on the i7 frame; each run saves row-aligned validation probabilities (`predictions_validation.npz` with `probs`, `y`, `innings_id`). Seed 7 trains on the laptop and seed 13 on the mini (D8). The validation split is 124,292 rows / 545 matches / 47 tournament blocks, 0 unmapped. Every stage 2 validation and test number is screening evidence (D2.1); the untouched cohort (frozen, 210 matches / 48,406 rows, `models/embeddings/seq_stage2/cohort/FROZEN.json`) is the only confirmatory read and is designed to be opened exactly once.

Registered contrasts (handoff § 5 step 7, Arm register `reference` column): each arm − `mlp`; `fox` − `fixed_decay`; `same_entity_k` − `recency_k` at the same k; `same_entity_unr` − `aligned_hist_rf`; `aligned_hist_rf` − `aligned_hist`; `aligned_hist` − `full`; `residual_t1` − `residual_mlp`. Only k = 30 has a registered recency control. Non-inferiority: upper 95% bound of (arm − mlp) below +0.002 on death overs and on chases. Uncertainty: tournament blocks, seeds and blocks resampled jointly, 2000 reps, bootstrap seed 29, ball-weighted, fewer than 10 blocks means descriptive only (invariant 7). Two estimands reported separately: (i) the registered checkpoint's own performance, (ii) across-training-seed robustness; two seeds speak to (ii) only weakly and must be labelled.

## Job 1 — write the D9 table (the k sweep, 2e)

Give me the numbered check rows (id, Check, Pass condition) exactly in the style of the existing D3/D5/D7/D8 tables, covering at minimum: which numbers are read and from where (full-precision per-seed validation LL from each configuration's `summary.yaml`, never a re-scored value); that all five `same_entity` k configurations must have both seeds before selection (D8.9); the selection rule (best validation LL; if no k beats k = 30 by more than 0.002, keep 30) and that ANY k including `unr` may win; that selection is validation-only and no test or cohort row is touched; how the chosen k is recorded and pinned; what happens to the other four k arms in the report; and the explicit statement that with two seeds the sweep is a screen, plus whatever guard you think is needed against reading a 0.002-scale difference as real when the seed spread is of the same order (say how to report the seed spread alongside).

## Job 2 — write the D10 table (statistics, gates, mechanism contrasts, report, 2f)

Same format. The orchestrator's handoff sketches 10.1-10.10; replace that sketch with the real thing. Cover at least: per-row log loss from the saved probabilities and how rows are aligned across arms (assert identical `y` and `innings_id` ordering before differencing); the tournament-block bootstrap exactly as registered, including how blocks are obtained for the validation split and the ≥10-block rule per slice; the two estimands and how a two-seed (ii) is labelled; the registered family map and the Holm step-down within each family, including what is confirmatory and what is exploratory and how rank-local intervals must be labelled (stage 1 was explicit that its Holm intervals were rank-local percentile intervals, not simultaneous); the two mechanism contrasts and the standing prohibition on inferring a difference from one arm being significant and another not; the non-inferiority gates and exactly how "upper confidence bound of the harm below +0.002" is computed and reported; the residual arms' qualifying contrast and the base-only comparison; the exploratory slices; the report generator (a script that reads every number from a file, writing `research/reports/embeddings/SEQ_STAGE2_REPORT.md`) and the requirement to restate every deviation, asymmetry and limitation without dropping one — including, from your gate 1 review, that the residual base logits are blocked cross-fitting rather than past-only forecasting, that `aligned_hist_rf − aligned_hist` measures wiring plus key construction, that machine is confounded with seed and no machine term is to be fitted, the xLSTM simplifications, A15 not adopted, the global prior not being as-of, and venue history not being recency-weighted; the family freeze and its pin; and the plain-language section.

Also specify the falsification conditions plainly: what result would mean "stale history is the cause", what would mean "ownership is the cause", what would mean "neither", and what must be true before any of those may be said at all. The plan's framing rule is that if no sequence arm beats the MLP CI-clean the conclusion is that this family at this resolution shows no further sequence gain, and that this is NOT a proof that the scoreboard summary suffices.

## Job 3 — rule on opening the untouched cohort tonight

The contract has the cohort opened once, after the family freeze, for any arm that clears the validation gates. Tonight's evidence is two seeds per arm, which the contract itself labels screening, and the plan's seed rule is five seeds with the 4/5-direction count. The orchestrator's instinct is that scoring the cohort on a two-seed screen spends the stage's only confirmatory read on evidence that cannot be confirmatory, and that the cohort should be held until the remaining three seeds are trained (the seed policy in D2.1 allows adding seeds later to whole families). Against that: the user wants the stage finished, and a cohort score is the only thing that would let any arm advance.

Rule on it: **open the cohort tonight, or defer it?** If you say defer, say exactly what the stage may then conclude and what its verdict status should be, and what precondition unlocks the cohort later. If you say open, say what must be frozen first, what may and may not be claimed from a two-seed cohort score, and how to keep the read genuinely single-shot. Either way, note that `research/log_verdict.py` will not be run tonight and the verdict is left for the user.

## Output

Section "D9 TABLE" (markdown table rows, ready to paste), section "D10 TABLE" (same), section "FALSIFICATION", section "COHORT RULING" ending with exactly one line "COHORT: OPEN" or "COHORT: DEFER — <reason in ten words or fewer>". Be concrete; prefer pass conditions a script can check over prose.
