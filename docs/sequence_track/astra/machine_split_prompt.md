You are Codex Astra. One focused design question about the stage 2 overnight training run of the sequence/embeddings track in this repository (read-only). You are separately reviewing the stage 2 code gate; this is a narrower question and the answer is needed before the night is launched.

Read: docs/sequence_track/stage2_acceptance.md (the contract: the "Arm register" table, D2.1, D2.6, D6's Result with the smoke timings, D8), docs/sequence_track/stage2_handoff_opus.md § 4 and § 5 step 5, docs/SEQUENCE_TRACK_PLAN.md § "Roles and execution model" (the machine-split table and its "The exact division is decided per stage when work starts" line) and § "Statistical rules", research/sequence_track/queue.yaml, research/sequence_track/run_queue.sh, scripts/sequence_track/queue_lib.py, scripts/sequence_track/retrain_stage2.py.

SITUATION. Sixteen registered configurations must each train at two seeds (7 and 13) — 32 runs. The plan's machine table assigns "stage 2-4 multi-seed training" to the Mac mini (16 GB unified, 10 cores, ~21 GB free, MPS, assumed 2-3x slower than the laptop), one job at a time, overnight, through run_queue.sh --machine mini. The laptop (48 GB, 15 cores, MPS) is assigned interactive work. The plan's Bookkeeping section says the exact division is decided per stage when work starts, so using both machines is permitted rather than excluded.

The night's long pole is xlstm: its one-epoch smoke was 62.7 s against 6.5 s for the token MLP (full D6 table in the acceptance file). Stage 1's registered 30-epoch/patience-3 runs early-stopped between epochs 10 and 28.

THE QUESTION. Should the thirty-two runs be split across the laptop and the mini to finish in one night, and if so, split how? Consider at least these options and say which you would register, with the reason:

  (A) Mini only, as the plan's table says. Accept that the night may not finish; the queue resumes the next night by config hash.
  (B) Split BY SEED: every configuration's seed 7 on one machine, every configuration's seed 13 on the other. Every arm-vs-arm contrast is then within-machine at each seed, and the seed-mean averages the two machines equally for every arm.
  (C) Split BY CONFIGURATION: some arms entirely on the laptop, others entirely on the mini (for example xlstm and lstm on the laptop because they are slowest).
  (D) Something else.

Points I specifically want your judgment on:

1. **Machine as a confound.** MPS is not bit-reproducible even within one machine, and the two machines differ in chip, core count and memory bandwidth. Under option (C), machine is perfectly confounded with arm for the split arms, which would corrupt exactly the contrasts the stage exists to measure. Under option (B), is the residual risk acceptable, and does it interact badly with the registered uncertainty contract — the paired seed-mean tournament-block bootstrap (D2.6: tournament blocks from load_competition_clusters, seeds and blocks resampled jointly, 2000 reps, seed 29), which pairs seeds across arms? State explicitly whether (B) leaves any registered contrast machine-confounded, and whether a machine term must be recorded anywhere.
2. **Non-determinism already accepted.** The stage records mps_bit_reproducible: false and every stage 2 number is screening evidence (two seeds, D2.1), with the untouched cohort as the only confirmatory read. Does that framing make a machine split cheaper than it would otherwise be, or is it irrelevant to this question?
3. **Mechanics.** queue.yaml tags every job `machine: mini` and run_queue.sh selects by tag. If you recommend a split, say exactly how to express it (two machine tags with the seed subset in the command via --seeds, a second queue file, or another way), how the driver's reuse check and the COMPLETE-marker-by-config-hash resumability behave when the same configuration's two seeds are produced on two machines and later rsynced into one output tree, and whether the resulting summary.yaml files merge correctly or collide.
4. **`expected_hours` per job.** The per-job timeout is 2x expected_hours, so too small a value kills legitimate work and too large a value lets a hung job burn the night. Give a concrete rule or per-job table derived from the D6 smoke timings and the stage 1 30-epoch references, for whichever machine each job lands on.
5. **Laptop-specific risks.** The laptop is the interactive machine: the orchestrator will be running codex reviews, pytest and git on it during the night, and the user may use it. Does that argue against putting training on it at all, and if it is used, what thread caps, memory floor and RSS cap should its queue carry (the mini's are floor 3 GB, cap 12 GB, OMP_NUM_THREADS=4)?
6. **Ordering.** If only part of the night finishes, which configurations must complete together for the stage to have any readable result at all — i.e. what is the minimum set whose absence makes the rest uninterpretable, given the registered contrasts (each arm minus mlp; fox minus fixed_decay; same_entity_k minus recency_k; same_entity_unr minus aligned_hist_rf; aligned_hist_rf minus aligned_hist; residual_t1 minus residual_mlp) and the k sweep with its "keep 30 unless another k wins by more than 0.002" rule?

Output: a short recommendation (one paragraph), then numbered MUST-DO items for the orchestrator, then any NOTE. End with exactly one line of the form "RECOMMENDATION: <A|B|C|D> — <ten words or fewer>".
