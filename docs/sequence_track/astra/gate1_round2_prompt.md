You are Codex Astra, reviewing round 2 of the stage 2 CODE GATE of the sequence/embeddings track (read-only). In round 1 you returned `VERDICT: AGREE WITH CHANGES` with 9 MUST-FIX, 4 SHOULD and 5 NOTE items. Every item has now been actioned. Your job: verify each closure against the code, and either sign off or name what is still wrong.

Read `docs/sequence_track/stage2_acceptance.md` IN FULL first — it is the contract and it now records your round 1 findings and their dispositions in the D11 table, plus the D9 and D10 tables written from your separate design round, and an amended D8 implementing the by-seed machine split you recommended. Then read the changed files: `scripts/sequence_track/recurrent_arms.py`, `scripts/transformer_t1.py`, `scripts/sequence_track/retrain_stage2.py`, `scripts/sequence_track/pin_stage2.py`, `scripts/sequence_track/pin_stage1.py`, `scripts/sequence_track/ownership_dependency_test.py`, `scripts/sequence_track/build_cohort_stage2.py`, `experiments/configs/seq_stage2_v1.yaml`, `research/sequence_track/queue_laptop.yaml`, `research/sequence_track/queue_mini.yaml`, `research/sequence_track/run_queue.sh`, `scripts/sequence_track/queue_lib.py`, the stage 2 test files under `scripts/tests/`, `scripts/tests/test_pin_stage1.py`, `docs/sequence_track/stage1_erratum_trainer_closure.md`, `docs/sequence_track/stage2_cohort_consumer_audit.md` and `docs/sequence_track/stage2_night_runbook.md`. Do not open anything under `data/golden/`, `data/forward_holdout/` or `models/embeddings/seq_stage2/cohort/`.

## What was done for each of your round 1 items

1. **MUST-FIX 1 (mLSTM denominator).** Changed to `max(|n'ᵀq|, exp(−m_new))` on the shifted states. The reference test was rewritten as a genuinely unstabilised float64 per-token loop (no stabiliser, gates applied directly, the paper's plain `max(|nᵀq|, 1)`); equality <1e-9 at d=32 double, <1e-5 at d=128 float32; a large-gate test asserts the unstabilised reference goes non-finite while the module stays finite. Mutation-checked: reverting to `clamp(min=1)` fails both equality tests at 3.06e-04. Verify the algebra and that the test is not the same formula written twice.
2. **MUST-FIX 2, 3, 4 and plan item 11 (driver).** A seed-independent training signature was added covering the training block, arm and `arm_params` including `key_construction`, frame identity, cache md5, base-logits digest and the implementation files the arm depends on; preflight now compares against the pin's `provenance:` block and refuses on mismatch or when it is absent, while skipping the cohort component so the mini (which has no cohort) can verify; completion now requires all four artefacts and is recorded atomically, with incomplete runs retried rather than refused or silently accepted; per-seed peak RSS is measured per child with the method recorded, and historically invalid values are marked invalid rather than reconstructed.
3. **MUST-FIX 5 (registration).** "Wiring alone" wording removed, the k rule now lets any k win including `unr`, and the family template was replaced with the explicit 15-family map you specified in the design round.
4. **MUST-FIX 6 (cohort eligibility audit).** Closed by `docs/sequence_track/stage2_cohort_consumer_audit.md` on provenance, opening no closed directory: the i7 cache was built 2026-07-26 03:13 and the frame 03:21, ten days before the 2026-08-05 export existed; the cache `_meta` records `source_json_file_count` 11264 / `source_match_count` 9519 (the frozen corpus, not the export's 10,189 files) and `source_json_mtime_max` 2026-04-19T04:14:12. Judge whether that is sufficient for D5.3's claim.
5. **MUST-FIX 7 (stage 1 verification).** Your option (c): an explicit historical-source verification mode resolving closure sources from the immutable blob at `2c7ea6f`, with strict live verification retained as the default and intentionally failing on the current tree. `experiments/configs/seq_stage1_sim_v1.yaml` was not re-pinned.
6. **MUST-FIX 8 (recurrent metadata).** `arm_params_block(..., extra=…)` merges the recurrent module's `cell` and `simplifications` into `metrics.json`, verified present in the re-smoked `lstm` and `xlstm` metrics. SHOULD 4's omitted mLSTM output gate was **restored** rather than recorded, in the block after the multi-head norm.
7. **MUST-FIX 9 (budgets) plus the machine split.** Two registered queue files, `queue_laptop.yaml` (seed 7) and `queue_mini.yaml` (seed 13), seed-specific `output_dir`, separate STOP files, and `expected_hours` from your formula with F = 3 on the mini and F = 1.5 on the laptop. xLSTM was recalibrated from its corrected 73.2 s smoke to 1.90 h (mini) and 0.95 h (laptop).
8. **SHOULD 1, 2, 3.** The dependency test now exits nonzero on a failed certificate or a failed positive control; the cohort builder refuses to rebuild once `FROZEN.json` exists unless explicitly overridden and reads exclusions from the recorded audit rather than reopening the closed files; the pin's source closure was expanded (or accurately renamed).
9. **NOTES 1, 2, 3, 5.** The relay-free own-outcome keys are retained per your NOTE 1. D7.4's Result uses your exact replacement wording. Your NOTE 3 qualification (blocked cross-fitting rather than past-only forecasting, caches and encoders not fold-refitted) is registered in D10.13 for the report's limitations. NOTE 5's reasoning is recorded in the erratum.

## Also new since round 1, and in scope for you

- The cohort is **DEFERRED** for this stage on your own ruling (`COHORT: DEFER`), recorded in D10.10 with unlock preconditions in D10.16. Tonight is a validation-only two-seed screen and `research/log_verdict.py` will not run.
- A laptop training worktree exists at `/Users/aryamangupta/CricML/MP_train_s7`, detached, with `data/xgb_data_i7`, `models/player_stats_cache_i7.sqlite`, `models/embeddings` and `models/xgb_i7_noweights_production` symlinked to the main checkout and `.venv` shared, so the main checkout can be edited while training runs. It will be re-pointed at the stage 2 commit before launch. Judge whether that arrangement is safe — in particular whether sharing `models/embeddings` (so both machines' outputs land in one tree) or sharing `.venv` creates any hazard, and whether anything in the trainer resolves a path in a way the symlinks would break.
- `scripts/sequence_track/stage2_stats.py` and `render_stage2_report.py` may or may not be present yet; they implement D9 and D10 and are NOT needed for the night's training. If present, review them; if absent, ignore them and say so.

## Output

For each round 1 item (MUST-FIX 1-9, SHOULD 1-4, NOTE 1-5) state CLOSED or STILL OPEN with file:line evidence. Then list any NEW MUST-FIX introduced by these changes — regressions matter more than anything else at this point, since the next action is a commit followed by an unattended overnight run on two machines. Then answer: is there anything that will fail, hang, corrupt an output tree, or silently skip when `run_queue.sh` runs these two queues unattended tonight, including the consolidation step where the two seed trees are rsynced together and the driver is re-invoked with `--seeds 7,13` to rewrite the summaries? End with exactly one line: "VERDICT: SIGN-OFF" or "VERDICT: AGREE WITH CHANGES" or "VERDICT: NO SIGN-OFF".
