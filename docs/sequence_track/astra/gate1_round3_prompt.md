You are Codex Astra, round 3 and final round of the stage 2 CODE GATE of the sequence/embeddings track (read-only). In round 2 you returned "Not ready for unattended launch" with 7 new MUST-FIX, 3 still-open round-1 items, and `VERDICT: AGREE WITH CHANGES`. Everything has been actioned. This is the last review before the commit and the unattended overnight launch, so the question is narrow: **is anything still going to break, corrupt, or silently no-op tonight?**

Read `docs/sequence_track/stage2_acceptance.md` first (its D11 table records your round 2 findings and their dispositions verbatim; D8 is amended). Then the changed files: `scripts/sequence_track/retrain_stage2.py`, `scripts/sequence_track/stage2_stats.py`, `scripts/sequence_track/render_stage2_report.py`, `research/sequence_track/queue_laptop.yaml`, `research/sequence_track/queue_mini.yaml`, `scripts/tests/test_retrain_stage2.py`, `scripts/tests/test_stage2_stats.py`, `scripts/tests/test_render_stage2_report.py`, `scripts/tests/test_queue_stage2.py`, `docs/sequence_track/stage2_night_runbook.md` (§ 3 and § 3b were rewritten), and `experiments/configs/seq_stage2_v1.yaml` (re-pinned). Do not open `data/golden/`, `data/forward_holdout/` or `models/embeddings/seq_stage2/cohort/`.

## What was done for each round 2 item

**new 1, worktree frame identity.** The signature's `frame` component now hashes `dir_configured` (the frame directory exactly as `data.directory` is written in the config, via `logical_frame_dir()`, refusing if preflight did not record it), the version, the whole `.feature_hash`, and per-split md5 / rows / date range; `stats_cache` hashes the configured manifest role plus the file md5. No resolved path enters any component; resolved paths are recorded outside the signature in `physical_paths` and `machine_provenance.paths`. `verify_checkpoint`'s frame check compares resolved physical directories rather than relative text. Two new tests assert identical signatures **and** identical per-component digests across a real-frame checkout root and a symlinked-frame root, and a consolidation of seed 7 written from the worktree root with seed 13 written from the main root.

**new 2, the runbook.** § 3 rewritten: explicit `--queue` on both machines, the two per-queue STOP files, the laptop launching from its worktree, `git -C <worktree> checkout --detach` to the commit, stopped-writer verification with `pgrep` before any rsync, and a new § 3b consolidation block. It also records your observation that the runner's exit status is not evidence of 32 completed runs.

**new 3, consolidation.** `--consolidate` added: refuses `--force` and `--dry-run`, never unlinks, its training loop is empty by construction, and on any incomplete or unverifiable run it names each one with its path and reason and directs re-queueing on the owning machine. The test that asserted the dangerous behaviour was replaced.

**new 4, machine provenance.** `machine_provenance()` captures hostname, machine label and its source, chip and how it was read, OS and release, platform, python and torch versions, MPS availability and build, device requested and device actually used, all four thread caps (unset recorded as null), repo root, whether that root is a worktree, and symlink/resolved status for the frame, cache, `models/embeddings` and out-root — into `run_record.json`, `COMPLETE.json` and `summary.yaml`, outside the signature. Both queue command lines now pass `--machine laptop` / `--machine mini` explicitly.

**new 5, templated k ids.** `k_configurations()` derives the k-to-configuration map from the config's `configurations` entries (`arm: same_entity` plus `params.k`), refusing gaps, duplicates and unregistered k; the real config maps `unr → same_entity_unr`. Tests cover an `unr` winner on real registered ids and assert `same_entity_kunr` never appears.

**new 6, unverified evidence.** `verify_run_admission()` is a single read-only gate every path passes before a number is read (config id and seed agreement across metrics, run record and completion record; registered arm and k; signature present and identical in both records; completion manifest bytes and md5 still matching disk). `verify_summary_provenance()` runs before `k_sweep` reads any LL; `summary_lls()` rejects duplicate seeds; `base_only_readout()` checks the npz md5, sidecar sha256, booster md5 and the sidecar's `parquet_md5` against the live parquet and the pin; `load_frame()` refuses pin drift or an absent pin.

**new 7, stale pin.** Re-written and re-verified after every agent landed: `sources 23 hashed`, `pin_stage2: OK`. Full artifact-free suite: **1684 passed, 56 deselected, 0 failed**.

**round-1 MUST-FIX 2.** `scripts/embeddings_e1.py` and `scripts/artifacts.py` added to every arm's implementation sources (closure derived by an AST import walk rooted at the two trainer modules, with a test that re-derives it). `source_comparison()` compares the live sha256 of the selected arms' training closure against the pin's recorded `provenance.sources.source_sha256` and refuses on mismatch; a training source with no pin entry is recorded in `training_sources_not_pinned` rather than silently passed; a `sources` block without a usable mapping refuses.

**round-1 MUST-FIX 3.** `model.pt` is loaded with `weights_only=True`, must be a non-empty state dict, and its parameter names must match the arm's wiring. `manifest_problems()` re-checks every artefact's size and md5 against the `COMPLETE.json` manifest on reuse. The three record states are explicit: absent (legacy, allowed), malformed (refused as unverifiable), disagreeing (refused naming the field).

**round-1 MUST-FIX 6.** Left open on your instruction and registered as a cohort-unlock prerequisite in D10.16; validation-only training proceeds.

## One deliberate deviation you must rule on

Your round-2 wording, relayed to the statistics implementer, asked that "the training signature must be identical across every run admitted into one comparison". The implementer reports that this cannot hold: the driver's signature includes `config_id`, `arm`, `arm_params` and `implementation`, so it is seed-independent but **arm-dependent** by construction, and the rule as stated would reject every real night. Implemented instead: **one signature per configuration across its seeds**, plus **component-by-component equality of `training_block`, `frame` and `stats_cache` across all arms**, with a test asserting arm-specific components may differ. Is that the right reading of your intent, and is the component set it holds fixed across arms sufficient to make cross-arm differencing safe? If you want more components pinned across arms, name them.

## Three things the implementer could not do as specified

1. `thin_pair` is reported **unavailable with a reason** rather than computed: the i7 frame carries no exposure column (`batter_balls_faced` / `bowler_balls_in_innings` are within-innings counters, and the eval kit that carries training-ball exposure is off for this stage). D10.7 permits exactly this, and a config block turns it on. Acceptable?
2. **D10.12 recertification is read, not run**: the report tabulates whatever is in `models/embeddings/seq_stage2/dependency/` and marks rows whose checkpoint lies under `smoke/` as the structural one-epoch certificate pending recertification. Running the recert on the trained checkpoints belongs to `ownership_dependency_test.py` and is a results-gate action. Acceptable as a plan, and is marking the smoke rows the right disclosure?
3. `innings_2` and `chase` select identical row sets on this frame (57,994 each); both are reported as registered rather than collapsed. Does that create a multiplicity or interpretation problem in the 15-family map, where `chase` is a gate slice?

## Output

Confirm or reject each round 2 item as CLOSED or STILL OPEN with file:line evidence. Rule on the deviation and the three items above. Then answer the launch question directly: **running `run_queue.sh --queue queue_laptop.yaml --machine laptop` from the worktree and `--queue queue_mini.yaml --machine mini` on the mini, unattended, then rsyncing seed 13 home and running `--consolidate --seeds 7,13` — does anything still fail, hang, corrupt a tree, silently skip, or admit bad evidence?** List any remaining MUST-FIX. End with exactly one line: "VERDICT: SIGN-OFF" or "VERDICT: AGREE WITH CHANGES" or "VERDICT: NO SIGN-OFF".
