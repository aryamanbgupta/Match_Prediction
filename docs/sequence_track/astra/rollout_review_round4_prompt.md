You are Codex Astra, round 4 (read-only, sign-off) on the standing rollout protocol, CricML branch embeddings-ladder. Rounds 1-3 are at docs/sequence_track/astra/rollout_review_round{1,2,3}.md. Revision 3 of docs/sequence_track/rollout_protocol.md closes your single round-3 MUST-FIX ([R3-M1], § 2.7 expansion bullet and every family in § 3 now names exactly one candidate, its primary is candidate minus reference, only a favourable primary expands that candidate, no reference arm ever expands, mlp_114 has its own family with primary mlp_114 − B, the residual A-only trigger is removed) and your NOTE 7 metric mismatch ([R3-N7], § 2.8 qualifies the mini on the paired shard-mean winner log-loss contrast against the Stage 1 two-batch range from timing/convergence_1b.json). Verify those two closures and that nothing else changed for the worse. Do not re-derive the design. Do not open data/golden, data/forward_holdout or models/embeddings/seq_stage2/cohort.

Output: numbered MUST-FIX (only what still blocks registration), then NOTE, each with file:line; then one plain-language paragraph for the user. End with exactly one line: "VERDICT: SIGN-OFF" or "VERDICT: AGREE WITH CHANGES" or "VERDICT: NO SIGN-OFF".
